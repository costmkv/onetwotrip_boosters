version: "2"
services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - ./data/db:/data/db

  web:
    build: .
    depends_on:
      - mongo
    ports:
      - "3000:3000"
